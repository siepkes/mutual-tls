spring:
  main:
    banner-mode: "off"

client:
  ssl:
    enabled: true
    key-store: signed-keystore.jks
    key-store-password: abc123
    key-password: abc123
    trust-store: truststore.jks
    trust-store-password: abc123

spring:
  main:
    banner-mode: "off"

client:
  ssl:
    enabled: true
#    key-alias: test-cert
#    key-store-type: PKCS12
    key-store: classpath:signed-keystore.jks
    key-store-password: abc123
    key-password: abc123
#    trust-store: classpath:identity.jks
#    trust-store-password: secret
    trust-store: classpath:truststore.jks
    trust-store-password: abc123